{
  "name": "castgorilla",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "macOS local-video to Chromecast player. Monorepo root.",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "fixtures": "bash scripts/gen-fixtures.sh"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
